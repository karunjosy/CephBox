db:
    host: postgres
    port: 5432
    name: ente_db
    user: pguser
    password: pgpass

s3:
    are_local_buckets: true
    b2-eu-cen:
        key: ACCESS_KEY # provide the access key of the bucket
        secret: SECRET_KEY # provide the secret key of the bucket
        endpoint: ENDPOINT # replace with the RGW s3 endpoint IP and port
        region: eu-central-2
            bucket: BUCKET_NAME # replace with bucket name
